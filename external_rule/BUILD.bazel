package(default_visibility = ["//visibility:public"])

load("@angular//:index.bzl", "ng_module")

ng_module(
    name = "external_rule",
    srcs = glob(["*.ts"]),
    module_name = "@external_rule/module",
)
